# Coneção Automatica com a api do serviço Twilio

Script para coneção com o serviço Twilio.

Primeiro instale as dependências:

```shell
npm i
```

Para iniciar o projeto basta:

```shell
node index.js
```
